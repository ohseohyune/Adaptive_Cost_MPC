# Pinned, not >=: mujoco 3.3.7 and 3.4.0 solve the same box-catch episode to
# ~1e-13 different contact forces, which flips 4/50 five-second HOLD success
# labels. Evaluation numbers are only comparable within one mujoco build --
# see tests/acmpc_evaluation_reproducibility_test.py.
mujoco==3.4.0
numpy>=1.24
scipy>=1.10
osqp>=0.6
torch>=2.0
matplotlib>=3.7
